{
    "version": 2,
    "buildCommand": "pnpm build",
    "builds": [
        {
            "src": "dist/app.cjs",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/app.cjs"
        }
    ]
}
